# SalleQ Client

Recovered React + TypeScript + Vite client for the SalleQ virtual waiting room.

Use this folder for local UI review and front-end iteration:

- `npm install`
- `npm run dev`
- `npm run build`

The FastAPI server in `src/app/server` will also serve a production build from `app/client/dist` when it exists.
